apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "calendar-chart.fullname" . }}-test-connection"
  labels:
    {{- include "calendar-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "calendar-chart.fullname" . }}:{{ .Values.calendarHttp.port }}']
  restartPolicy: Never
